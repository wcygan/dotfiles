{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Run install-safely.ts",
      "type": "shell",
      "command": "deno run --allow-read --allow-write --allow-run --allow-env install-safely.ts",
      "problemMatcher": [],
      "detail": "Runs the dotfiles installation script."
    }
  ]
}
