use flake

# Optional: Set up local environment variables for Docusaurus
# export BROWSER=none  # Prevent auto-opening browser on npm start
# export PORT=3000     # Default port for Docusaurus

# Optional: Auto-install dependencies if node_modules is missing
if [ ! -d "node_modules" ]; then
  echo "📦 Installing npm dependencies..."
  npm install
fi

# Optional: Show current git branch in prompt when in this directory
# export PS1="[docs:\$(git branch --show-current)] \$PS1"
