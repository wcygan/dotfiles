# Simple direnv - no Nix, no builds, just npm
PATH_add node_modules/.bin

# Auto-install dependencies if missing
if [ ! -d "node_modules" ]; then
  echo "📦 Installing npm dependencies..."
  npm install
fi

npm start